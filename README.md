# ZoomAndPanController

Recharts 사용자가 기존 차트 코드를 거의 바꾸지 않고 붙일 수 있는 Zoom & Pan 컨트롤러 라이브러리.

## 개발

- `npm run dev` — 플레이그라운드 (Vite)
- `npm test` — Vitest 테스트
- `npm run storybook` — Storybook (포트 6006)
- `npm run build` — 라이브러리 빌드 (ESM + 타입 선언)
